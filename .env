# Default road-data location. Served from a sibling repo via GitHub Pages.
# Override in .env.local (gitignored) to fetch from a local directory instead.
# See docs/DATA-HOSTING.md for details.
VITE_ROADS_BASE_URL=https://rinshannkaihou.github.io/city-scale-comparison-data/
